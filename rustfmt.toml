edition = "2021"
max_width = 100
use_small_heuristics = "Max"
# NOTE: imports_granularity / group_imports require a nightly toolchain and
# are intentionally NOT set here, so `cargo fmt --check` is reproducible
# on the stable toolchain used by CI (dtolnay/rust-toolchain@1.96.0).
